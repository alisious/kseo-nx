<UserControl x:Class="kseo_nx.Views.ReservationWizardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1700">
    <Grid>
        <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="1200"/>
                <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"  Grid.Row="1" Grid.Column="1">
            <Button x:Name="Cancel" Width="120">Anuluj</Button>
            <Button x:Name="GoPrevious" Width="120">Cofnij</Button>
            <Button x:Name="GoNext" Width="120">Dalej</Button>
            <Button x:Name="Finish" Width="120">Zakończ</Button>
        </StackPanel>
        <ContentControl x:Name="ActiveItem" Grid.Row="0" Grid.Column="1"/>
    </Grid>
</UserControl>
