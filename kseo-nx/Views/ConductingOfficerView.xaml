<UserControl x:Class="kseo_nx.Views.ConductingOfficerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" Width="500">
    <StackPanel VerticalAlignment="Stretch"  >
        <DockPanel>
            <Label Width="150">Nazwisko:</Label>
            <ComboBox x:Name="OfficerList" SelectedItem="{Binding Officer}" Margin="2" IsTextSearchEnabled="True" IsEditable="True"/>
        </DockPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="Remove" Width="60" Style="{StaticResource HyperLinkButtonStyle}" VerticalContentAlignment="Center">Usuń</Button>
        </StackPanel>
    </StackPanel>
</UserControl>
