<UserControl x:Class="kseo_nx.Views.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="1800"
             Height="1000"
             Width="1800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Name="BackToStartView">Powrót do strony głównej.</Button>
            <Button Name="CloseApplication">Zamknij</Button>
            <Label>Operator:</Label>
            <Label x:Name="UserName"/>
            <Label>Data operacji:</Label>
            <Label x:Name="DateOfOperations"/>
        </StackPanel>
        
        <ContentControl Grid.Row="1" Name="ActiveItem"/>
    </Grid>
</UserControl>
